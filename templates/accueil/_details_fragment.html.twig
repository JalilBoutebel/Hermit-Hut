<div class="details-content">
    <h2>{{ jeu.libelle }}</h2>
    <img src="{{ asset(jeu.image) }}" alt="{{ jeu.libelle }}" class="details-img">
    <p><strong>Catégorie :</strong> {{ jeu.categorie.libelle }}</p>
    <p>{{ jeu.description }}</p>
    <p class="price">💰 {{ jeu.prix }} €</p>
    <a href="{{ path('app_add_panier', {id: jeu.id}) }}" class="btn-add">🛒 Ajouter au panier</a>
</div>

<style>
.details-content {
    text-align: center;
    color: #fff;
    font-family: 'Orbitron', sans-serif;
}
.details-img {
    max-width: 300px;
    border-radius: 12px;
    box-shadow: 0 0 15px #00fff7;
    margin: 15px 0;
}
.price {
    color: #00fff7;
    font-weight: bold;
    margin-top: 15px;
    font-size: 1.3rem;
}
.btn-add {
    background: #00b4ff;
    color: white;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
}
.btn-add:hover {
    background: #00fff7;
    color: #000;
}
</style>
