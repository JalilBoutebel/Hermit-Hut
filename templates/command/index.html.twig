{% extends 'base.html.twig' %}

{% block title %}📜 Mes commandes - Hermit's Hut{% endblock %}

{% block body %}
<div class="command-wrapper">
    <div class="command-container">
        <h1>📜 Mes commandes</h1>
        
        <p class="welcome-text">Bienvenue {{ app.user.prenom }} ! 🎮</p>
        
        {% if app.user.commandes is empty %}
            <div class="empty-state">
                <p class="empty-message">Vous n'avez pas encore passé de commandes.</p>
                <a href="{{ path('app_accueil') }}" class="btn-primary">Découvrir nos jeux 🎯</a>
            </div>
        {% else %}
            <div class="commandes-list">
                {% for commande in app.user.commandes %}
                    <a href="{{ path('app_command_details', {id: commande.id}) }}" class="commande-card">
                        <div class="commande-header">
                            <h3>🎮 Commande #{{ commande.id }}</h3>
                            <span class="commande-date">{{ commande.dateCommande|date('d/m/Y à H:i') }}</span>
                        </div>
                        
                        <div class="commande-body">
                            <div class="commande-info">
                                <span class="label">Statut :</span>
                                <span class="statut statut-{{ commande.statut }}">
                                    {% if commande.statut == 'en_attente' %}
                                        ⏳ En attente
                                    {% elseif commande.statut == 'validee' %}
                                        ✅ Validée
                                    {% elseif commande.statut == 'expediee' %}
                                        📦 Expédiée
                                    {% elseif commande.statut == 'livree' %}
                                        🎉 Livrée
                                    {% elseif commande.statut == 'annulee' %}
                                        ❌ Annulée
                                    {% endif %}
                                </span>
                            </div>
                            
                            <div class="commande-info">
                                <span class="label">Total :</span>
                                <span class="total">{{ commande.total }} €</span>
                            </div>
                        </div>
                        
                        <div class="commande-footer">
                            <span class="voir-details">Voir les détails →</span>
                        </div>
                    </a>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>

<style>
/* 🌌 Arrière-plan global */
body {
    background-attachment: fixed;
    font-family: 'Orbitron', sans-serif;
    color: #fff;
    min-height: 100vh;
    margin: 0;
}

/* 🌫️ Conteneur centré */
.command-wrapper {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding: 60px 15px;
}

/* 📜 Bloc des commandes */
.command-container {
    width: 100%;
    max-width: 1000px;
    background: rgba(0, 0, 0, 0.65);
    border-radius: 15px;
    padding: 30px 40px;
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.25);
    backdrop-filter: blur(10px);
}

/* 🧠 Titre */
.command-container h1 {
    text-align: center;
    color: #20dd75ff;
    margin-bottom: 20px;
    text-shadow: 0 0 10px #11cc75ff;
    font-weight: 700;
}

.welcome-text {
    text-align: center;
    color: #8dd617ff;
    font-size: 1.2rem;
    margin-bottom: 40px;
}

/* 🎯 État vide */
.empty-state {
    text-align: center;
    padding: 60px 20px;
}

.empty-message {
    color: #ccc;
    font-style: italic;
    margin-bottom: 30px;
    font-size: 1.1rem;
}

/* 📋 Liste des commandes */
.commandes-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

/* 🎴 Carte de commande */
.commande-card {
    display: block;
    background: rgba(0, 0, 0, 0.4);
    border: 2px solid rgba(13, 211, 79, 0.3);
    border-radius: 12px;
    padding: 20px;
    text-decoration: none;
    transition: all 0.3s ease;
}

.commande-card:hover {
    background: rgba(32, 221, 117, 0.1);
    border-color: #20dd75ff;
    transform: translateY(-3px);
    box-shadow: 0 0 20px rgba(32, 221, 117, 0.3);
}

/* 📌 En-tête de la carte */
.commande-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    padding-bottom: 15px;
    border-bottom: 1px solid rgba(13, 211, 79, 0.2);
}

.commande-header h3 {
    color: #0dd34fff;
    margin: 0;
    font-size: 1.3rem;
}

.commande-date {
    color: #8dd617ff;
    font-size: 0.9rem;
}

/* 📝 Corps de la carte */
.commande-body {
    display: flex;
    gap: 30px;
    margin-bottom: 15px;
}

.commande-info {
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.label {
    color: #0dd34fff;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.statut {
    padding: 8px 15px;
    border-radius: 8px;
    font-weight: bold;
    font-size: 0.95rem;
    display: inline-block;
}

.statut-en_attente {
    background: rgba(255, 193, 7, 0.2);
    color: #ffc107;
}

.statut-validee {
    background: rgba(76, 175, 80, 0.2);
    color: #4caf50;
}

.statut-expediee {
    background: rgba(33, 150, 243, 0.2);
    color: #2196f3;
}

.statut-livree {
    background: rgba(32, 221, 117, 0.2);
    color: #20dd75ff;
}

.statut-annulee {
    background: rgba(244, 67, 54, 0.2);
    color: #f44336;
}

.total {
    color: #8dd617ff;
    font-size: 1.3rem;
    font-weight: bold;
    text-shadow: 0 0 8px rgba(141, 214, 23, 0.5);
}

/* 👣 Pied de la carte */
.commande-footer {
    text-align: right;
}

.voir-details {
    color: #0dd34fff;
    font-weight: 600;
    transition: all 0.3s ease;
}

.commande-card:hover .voir-details {
    color: #91f71cff;
    text-shadow: 0 0 8px #0be97aff;
}

/* 🎮 Bouton principal */
.btn-primary {
    display: inline-block;
    padding: 15px 40px;
    background: linear-gradient(135deg, #20dd75ff, #0dd34fff);
    color: #000;
    font-weight: bold;
    font-size: 1.1rem;
    text-decoration: none;
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(32, 221, 117, 0.5);
    transition: all 0.3s ease;
}

.btn-primary:hover {
    background: linear-gradient(135deg, #91f71cff, #20dd75ff);
    transform: translateY(-3px);
    box-shadow: 0 0 30px rgba(145, 247, 28, 0.7);
}

/* 📱 Responsive */
@media (max-width: 768px) {
    .command-container {
        padding: 20px;
    }
    
    .commande-header {
        flex-direction: column;
        gap: 10px;
        align-items: flex-start;
    }
    
    .commande-body {
        flex-direction: column;
        gap: 15px;
    }
}
</style>
{% endblock %}